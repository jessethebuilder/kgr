<!--<div class="container">-->
  <!--<div class="row">-->
    <!--<div class="col-md-12">-->
      <div id='top_nav' class='navbar navbar-default navbar-fixed-top' role='navigation'>
        <div class="container">
          <div class='navbar-header'>
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#top_nav_body">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
            <%= link_to 'Killer Green Radio!', root_path, :class => 'navbar-brand' %>
          </div>

          <div id='top_nav_body' class='collapse navbar-collapse'>
            <ul class='nav navbar-nav navbar-right'>


              <li>
                <%= link_to 'Home', root_path %>
              </li>

              <li>
                <%= link_to 'News', news_stories_path %>
              </li>

              <li>
                <%= link_to 'DJs', djs_path %>
              </li>

              <li>
                <%= link_to 'Events', events_path %>
              </li>

              <% if user_is_admin? %>
                <li classs="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown">Admin <b class="caret"></b></a>
                  <ul class="dropdown-menu">
                    <!--<li class="divider"></li>-->
                  </ul>
                </li>
              <% end %>

              <li>
                <% if user_signed_in? %>
                  <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>
                <% else %>
                  <%= link_to 'Login', new_user_session_path %>
                <% end %>
              </li>
            </ul>
          </div>
        </div>
      </div>
    <!--</div>-->
  <!--</div>-->
<!--</div>-->

<script>
  function padBodyForTopNav(){
    $('body').css('padding-top', $('#top_nav').height() + 1);

    $(window).resize(function(){
      $('body').css('padding-top', $('#top_nav').height() + 1);
    })
  }

  padBodyForTopNav();
</script>